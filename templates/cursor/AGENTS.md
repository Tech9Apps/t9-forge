# Project Agent Guide

This file is generated by `claude-code-toolkit` for Cursor.

## Project Summary
- Name: `{{PROJECT_NAME}}`
- Stack: `{{STACK_SUMMARY}}`

## Required Workflow
1. Create or update a plan before implementation for non-trivial changes.
2. Run `/plan-verify` and resolve all critical issues before coding.
3. Implement with focused, test-backed steps.
4. Run project verification checks before finalizing.
5. Ask for confirmation before high-impact or broad-scope writes.

## Workflow Skills
- Planning: `/plan`, `/plan-verify`, `/plan-ceo-review`, `/plan-eng-review`, `/plan-design-review`
- Quality and release: `/review`, `/ship`, `/qa`, `/qa-only`, `/qa-design-review`
- Browser/session/runtime: `/browse`, `/setup-browser-cookies`
- Delivery and learning: `/document-release`, `/retro`

## Capability-Gated Behavior
- Browser-backed workflows must use Cursor browser tooling when available.
- If browser/runtime capabilities are missing, fall back to report-only mode and state the missing prerequisites.
- Keep target behavior equivalent to Claude workflows where possible, but do not invent `.claude/*` runtime artifacts for Cursor.

## Verification Commands
- Build: `{{BUILD_COMMAND}}`
- Lint: `{{LINT_COMMAND}}`
- Format: `{{FORMAT_COMMAND}}`
- Typecheck: `{{TYPECHECK_COMMAND}}`
- Test: `{{TEST_ALL_COMMAND}}`

## Rule References
- `@.cursor/rules/architecture.mdc`
- `@.cursor/rules/api-and-data.mdc`
- `@.cursor/rules/testing.mdc`
- `@.cursor/rules/workflow.mdc`

## Notes
- Keep reasoning explicit over clever shortcuts.
- Skip irrelevant workflows for capabilities the project does not use.
