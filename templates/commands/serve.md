# Start Development Server

Start the project's development server(s).

## Instructions

Start the development server:

```bash
{{DEV_SERVER_COMMAND}}
```

<!-- Examples:
  Node: npm run dev
  Python/Django: python manage.py runserver
  Python/FastAPI: uvicorn main:app --reload
  Rust/Actix: cargo watch -x run
  Go: go run . or air
  Ruby/Rails: bin/rails server
  Docker: docker-compose up
-->

If the project has multiple services (e.g., frontend + backend), start them appropriately:

{{ADDITIONAL_SERVICES}}

<!-- Examples:
  - npm run dev:frontend & npm run dev:backend
  - docker-compose up
  - Use separate terminal commands
-->

Report the URL(s) where the server is accessible once started.

$ARGUMENTS
